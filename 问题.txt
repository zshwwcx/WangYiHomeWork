问题：
1. 浮点常量需要考虑其他进制的吗？比如十六进制的浮点常量
2. 只需要考虑那些定义了之后可以被使用的宏吗？有一些宏定义方式，如果没有被使用，也不会报错比如 #define data_3 data2，如果在程序中有使用data_3会报错data2未定义，但不调用data3则不会报错，像#define data 5f，这样的也是，不调用data5就不会报错
3. 考虑宏拼接吗？
4. 2L这样的变量是算浮点常量还是算整数常量，我看微软的那个文档里面，在整数常量和浮点常量里都出现了这样的定义，而typeid()给出的类型是long，所以碰到这样的宏是算哪种常量？
5. 还用从c11起，字符常量前缀支持u了，这个也要考虑吗？
6. 碰到多字符常量，比如 #define data 'ABCD', 是不是也是直接输出'ABCD'对应的int值？单字符常量是返回的ascii值，那多字符常量是返回什么值？
7. 会出现中文字符串吗？

